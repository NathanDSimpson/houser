insert into property (
    name,
    address,
    city,
    state,
    zip,
    img,
    mortgage,
    rent
    ) 
    values 
    (
        ${name}, 
        ${address}, 
        ${city}, 
        ${state}, 
        ${zip}, 
        ${img}, 
        ${mortgage}, 
        ${rent}
    );